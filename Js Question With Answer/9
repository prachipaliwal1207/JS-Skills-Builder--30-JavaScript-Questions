9. Create an object with the following characteristics: 4 wheels (named wheels as integer), 100PS power (named power and integer), 3 doors (named doors as integer). Return the built car.
* Code -> function buildingACar() {
  const demo = {
     wheels:4,
     power:100,
     doors:3
   };
  return demo;
}


//This function returns an object representing a car with 4 wheels, 100 horsepower, and 3 doors.