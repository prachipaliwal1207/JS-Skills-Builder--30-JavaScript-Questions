14. Code a for loop that runs through all numbers from 0-99 and append all numbers divisible by seven into a string. Return this string.
* Code -> function easyLoop() {
let a="";
let b="";
  for(let i=0;i<=99;i++){
    if(i%7==0){
       b = i+"";
      a+=b;
    }
  }
  return a;
}

//This function loops through numbers from 0 to 99 and appends those divisible by 7 into a string.