2. Given are two strings password and password_repeat. Check if the password is secure by the following critera:
- both passwords must match
- password must be at least 20 chars

Return true only if all criteria are met, otherwise return false
* Code -> function checkPassword(password, password_repeat) {
  if(password.length>=20){
        if(password == password_repeat){
      return true;
    }else{
          return false;
    }
  }else{
          return false;
    }
}

//This function checks if the password is at least 20 characters long and if it matches the repeated password. If both conditions are met, it returns true, otherwise false.